using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Battery : ComponentSS
{
    // Start is called before the first frame update
    void Start()
    {
        // maxEnergy += 1;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
